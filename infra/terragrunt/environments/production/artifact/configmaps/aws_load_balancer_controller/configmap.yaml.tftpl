---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ${name}
  namespace: ${namespace}
data:
  values.yaml: |
    clusterName: ${cluster}
    serviceAccount:
      annotations:
        eks.amazonaws.com/role-arn: ${role_arn}
