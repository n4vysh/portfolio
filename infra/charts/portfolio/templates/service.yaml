---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: nginx
spec:
  ports:
    - port: {{ .Values.port }}
      name: main
    - port: {{ .Values.status.port }}
      name: status
  selector:
    app: nginx
